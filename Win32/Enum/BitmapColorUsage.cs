namespace Windows.Enum
{
    public enum BitmapColorUsage
    {
        /// <summary>The color table contains 16-bit indexes into the currently selected palette.</summary>
        PaletteIndecies = 1,
        /// <summary>The color table contains RGB values.</summary>
        RgbValues = 0
    }
}
